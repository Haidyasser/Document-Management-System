spring:
  application:
    name:Document Management system
  datasource:
    url: ${MYSQL_URL}
    username: ${USERNAME}
    password: ${PASSWORD}
  jps:
    hibernate:
      ddl:
        auto:
          update
    show:
      sql:
        true